// Preserve Boxmate's location, photo, distance and directions interaction.
const locations=[{lat:40.4416,lng:-79.9430,address:'5032 Forbes Ave, Pittsburgh',photo:'https://images.unsplash.com/photo-1441986300917-64674bd600d8?w=500&auto=format&fit=crop'},{lat:40.4444,lng:-79.9445,address:'4765 Forbes Ave, Pittsburgh',photo:'https://images.unsplash.com/photo-1566073771259-6a8506099945?w=500&auto=format&fit=crop'},{lat:40.4436,lng:-79.9459,address:'5000 Forbes Ave, Pittsburgh',photo:'https://images.unsplash.com/photo-1501339847302-ac426a4a7cbb?w=500&auto=format&fit=crop'}];
let userPoint={lat:40.4442,lng:-79.9962},actualLocation=false;
function miles(a,b){const r=Math.PI/180,dlat=(b.lat-a.lat)*r,dlng=(b.lng-a.lng)*r;const q=Math.sin(dlat/2)**2+Math.cos(a.lat*r)*Math.cos(b.lat*r)*Math.sin(dlng/2)**2;return (3958.8*2*Math.atan2(Math.sqrt(q),Math.sqrt(1-q))).toFixed(1)}
const oldPlaceCards=placeCards;
placeCards=function(){return places.map((p,i)=>`<button class="place business-card ${selected===i?'selected':''}" data-place="${i}" aria-pressed="${selected===i}"><img src="${locations[i].photo}" alt="Illustrative ${p.type.toLowerCase()} photo" loading="lazy"><div><span class="tag">${p.tag}</span><div class="line"><h3>${p.name}</h3><strong>${money(p.cap)}<small> / day</small></strong></div><p>${locations[i].address}</p><p><strong>${miles(userPoint,locations[i])} miles</strong> from ${actualLocation?'you':'sample start'} · straight-line</p><p>${p.time} min added walking · illustrative</p><span class="notice">Fictional business · illustrative photo</span></div></button>`).join('')};
const originalRender=render;
render=function(){originalRender();document.querySelector('main').classList.toggle('location-booking',step===1&&mode==='guided');if(step!==1||mode!=='guided')return;const section=document.querySelector('#screen .layout');if(!section)return;const map=document.createElement('div');map.className='location-map';const p=locations[selected];map.innerHTML=`<div class="map-toolbar"><span>${actualLocation?'Your location':'Sample starting location'} → selected business</span><button id="locate">Use my location</button></div><div class="live-map" role="region" aria-label="Storage locations map"></div><div class="map-caption"><span>${places[selected].name}</span><a href="https://www.google.com/maps/dir/?api=1&origin=${userPoint.lat},${userPoint.lng}&destination=${p.lat},${p.lng}&travelmode=walking" target="_blank" rel="noopener">Walking directions ↗</a></div><p class="notice">Demo coordinates. Map updates when you select a business. Trip detours are illustrative.</p><p id="geoStatus" role="status"></p>`;section.append(map);mountStorageMap(map.querySelector(".live-map"));document.querySelector('#locate').onclick=()=>{if(!navigator.geolocation){document.querySelector('#geoStatus').textContent='Location is unavailable. Enter your starting point instead.';return}document.querySelector('#geoStatus').textContent='Waiting for location permission…';navigator.geolocation.getCurrentPosition(position=>{userPoint={lat:position.coords.latitude,lng:position.coords.longitude};actualLocation=true;trip.start='Your current location';render()},()=>document.querySelector('#geoStatus').textContent='Location was unavailable or declined. The sample starting point is still selected.',{timeout:10000})}};
